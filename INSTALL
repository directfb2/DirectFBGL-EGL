DirectFBGL-EGL is built using Meson.

First, configure the build:

  $ meson setup build/

Start the build with:

  $ meson compile -C build/

And install DirectFBGL-EGL using:

  $ meson install -C build/
